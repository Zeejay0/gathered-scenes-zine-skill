# Third-party notices

The optional face-harmonization extension bundles Google's MediaPipe Face Landmarker float16 v1 model. Its published source URL, documentation URL, and SHA-256 checksum are recorded in `assets/face-harmonization-model.json`.

The optional Node.js and Python packages are governed by their respective licenses. Package versions are recorded in `package-lock.json` and `requirements-face.txt`.
